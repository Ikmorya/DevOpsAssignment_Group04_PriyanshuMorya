events { worker_connections 1024; }
http {
  server {
    listen 80;
    location / {
      proxy_pass http://web:3000;
    }
  }
}

    location /static/ {
      expires 30d;
      add_header Cache-Control "public, no-transform";
    }
